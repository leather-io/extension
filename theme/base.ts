import { definePreset } from '@pandacss/dev';

export default definePreset({
  theme: {
    tokens: {
      colors: {
        darkModeBrown: {
          2: { value: '#2C2A24' },
        },
      },
    },
  },
});
