import { token } from 'leather-styles/tokens';

// virtuosoHeight = calc(InteractiveItem height - negative margin) 72px - 24px = 58
export const virtuosoHeight = 58;

export const virtuosoStyles = {
  paddingBottom: token('spacing.space.05'),
};
