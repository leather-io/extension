import { Button } from '@stacks/ui';

export function SendAllButton() {
  return (
    <Button
      borderRadius="12px"
      fontSize={0}
      height="32px"
      onClick={() => {}}
      mode="tertiary"
      px="tight"
      type="button"
      width="70px"
    >
      Send all
    </Button>
  );
}
